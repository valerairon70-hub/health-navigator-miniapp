# Правила работы с проектом

## Общее
- Это Telegram Mini App — все решения принимаем с учётом мобильного формата внутри Telegram
- Перед созданием нового файла — сначала объясни, что планируешь сделать и зачем

## Как запустить локально

```bash
cd mvp_extracted
npm install
npm run dev
```

Приложение запускается на `http://localhost:3000`

Перед запуском создай файл `.env` на основе `.env.example`:
```
TELEGRAM_BOT_TOKEN=...  # хранится в Vercel; в коде фронтенда не используется (SPA без бэкенда)
GEMINI_API_KEY=...      # зарезервировано на будущее, сейчас не используется
APP_URL=http://localhost:3000
```

Для деплоя: пушим в `main` → Vercel деплоит автоматически.

## Стиль текста в приложении
- Простой, дружеский, без канцелярита
- Писать так, как говорит живой человек — не "осуществить взаимодействие", а "написать"
